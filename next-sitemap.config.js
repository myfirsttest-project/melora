/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://meloraplayschool.in',
    generateRobotsTxt: true, // generate robots.txt
    changefreq: 'monthly',
    priority: 0.7,
    sitemapSize: 5000,
    trailingSlash: true,     // keep URLs with /
};
